name: Notify owners by email on push

on:
  push:
    branches:
      - main
      - saleh

jobs:
  send-email:
    if: contains(github.event.head_commit.message, '[notify]')
    runs-on: ubuntu-latest

    steps:
      - name: Read update message
        id: update_message
        shell: bash
        run: |
          if [ -f update-message.txt ]; then
            echo "message<<EOF" >> $GITHUB_OUTPUT
            cat update-message.txt >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "message=Aucun message detaille fourni." >> $GITHUB_OUTPUT
          fi
      - name: Send email notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: Saleh738297@gmail.com
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "ðŸš€ Nouvelle mise a jour - Yamani Food"
          to: "aaalkhulagi@gmail.com,a774264429@gmail.com"
          from: "Yamani Food Updates <Saleh738297@gmail.com>"
          body: |
            Bonjour ðŸ‘‹

            ðŸš€ Une nouvelle mise a jour importante a ete appliquee sur le site Yamani Food.

            ðŸ“Œ Travail realise :
            ${{ github.event.head_commit.message }}

📝 Details de la mise a jour :
${{ steps.update_message.outputs.message }}

            ðŸ“Š Informations du projet :
            â€¢ Projet : Yamani Food
            â€¢ Branche : ${{ github.ref_name }}
            â€¢ Developpeur : ${{ github.actor }}

            ðŸŽ¯ Objectif :
            Le site continue dâ€™evoluer avec des ameliorations, corrections et optimisations regulieres afin dâ€™ameliorer lâ€™experience client, la stabilite et les performances.

            ðŸ”” Notification envoyee uniquement pour les mises a jour importantes.

            Merci pour votre confiance ðŸ™

            â€” Yamani Food

